import { QueryInterface } from 'sequelize';
import * as faker from 'faker';

export default class UsersTableSeeder20191127155519 {
    async up(query: QueryInterface) {
        
    }

    async down(query: QueryInterface) {
        
    }
}